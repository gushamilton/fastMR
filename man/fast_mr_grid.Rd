% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastmr.R
\name{fast_mr_grid}
\alias{fast_mr_grid}
\title{Run every exposure/outcome pair in a shared exact grid}
\usage{
fast_mr_grid(
  exposure_beta,
  outcome_beta,
  exposure_se,
  outcome_se,
  methods = c("ivw", "egger", "weighted_median", "simple_mode", "weighted_mode"),
  nboot = 1000,
  seed = NULL,
  threads = 1,
  output = NULL,
  ...
)
}
\arguments{
\item{exposure_beta}{Exposure effect matrix, exposures by SNP.}

\item{outcome_beta}{Outcome effect matrix, outcomes by SNP.}

\item{exposure_se}{Exposure standard-error matrix.}

\item{outcome_se}{Outcome standard-error matrix.}

\item{methods}{Character vector of method codes.}

\item{nboot}{Number of bootstrap draws.}

\item{seed}{Optional integer seed.}

\item{threads}{Maximum native worker count.}

\item{output}{Optional path for a Zstandard-compressed Parquet copy of the
result. The path must not already exist; use \code{\link{fast_write_parquet}} when
an overwrite or another compression codec is required.}

\item{...}{Optional \code{phi} bandwidth multiplier for mode methods and \code{penk}
penalty multiplier for penalised weighted median (default 20).}
}
\value{
A tidy data frame with one row per method and grid pair.
}
\description{
Matrix rows are exposures/outcomes and columns are shared SNPs. Results are
returned in exposure-major, outcome-minor order. R's column-major matrices
are copied once at the C++ boundary into contiguous row-major pair layouts.
}
